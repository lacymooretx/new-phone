<configuration name="xml_curl.conf" description="cURL XML Gateway">
  <bindings>
    <binding name="directory">
      <param name="gateway-url" value="http://api:8010/freeswitch/directory" bindings="directory"/>
      <param name="method" value="POST"/>
      <param name="timeout" value="5"/>
    </binding>
    <binding name="dialplan">
      <param name="gateway-url" value="http://api:8010/freeswitch/dialplan" bindings="dialplan"/>
      <param name="method" value="POST"/>
      <param name="timeout" value="5"/>
    </binding>
    <!-- Configuration section NOT bound to xml_curl.
         FreeSWITCH uses local XML files for sofia profiles, modules, etc.
         Gateway provisioning handled via sofia_profile_rescan + config sync. -->
  </bindings>
</configuration>
