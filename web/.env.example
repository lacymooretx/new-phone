# API base URL (only needed for non-Docker dev when not using Vite proxy)
# VITE_API_URL=http://localhost:8000

# Google Maps API key (enables address autocomplete in Sites and E911 forms)
# VITE_GOOGLE_MAPS_API_KEY=
